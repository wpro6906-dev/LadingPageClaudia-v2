# ─── URL del backend (API en Render) ─────────────────────────────────────────
# En desarrollo local (Replit): dejar en blanco — el frontend usa rutas relativas
# En producción (Vercel): poner la URL de Render, SIN barra final
# Ejemplo: VITE_API_URL=https://claudia-alzate-api.onrender.com
VITE_API_URL=
